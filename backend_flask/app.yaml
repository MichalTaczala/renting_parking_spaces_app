runtime: python39
entrypoint: gunicorn -b :$PORT main:app

handlers:
  - url: /.*
    script: auto
    secure: always

env_variables:
  DB_USER: postgres
  DB_PASS: root1234
  DB_NAME: my_app_db
  INSTANCE_CONNECTION_NAME: parking-app-v2:us-central1:my-sql-instance
